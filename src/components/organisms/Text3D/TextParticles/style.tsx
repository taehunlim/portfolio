import styled from '@emotion/styled';

const Container = styled.div`
   width: 100%;
   padding-top: 100%;
   position: relative;
   canvas {
      position: absolute;
      top: 0;
   }
`;

export { Container };
